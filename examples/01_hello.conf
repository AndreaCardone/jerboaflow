# 01_hello.conf -- the smallest useful pipeline.
#
# A generator emits 5 "hello" packets at 200ms intervals; a printer
# writes each to stdout. The flow exits cleanly once the generator
# is exhausted.
#
# Run: ./jerboa examples/01_hello.conf

gen  generator 200 5 hello
out  printer   stdout

gen -> out
